# Secreto para firmar las cookies de sesión (obligatorio en producción).
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GG_SESSION_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio

# Credenciales del panel de administración (usuario semilla).
GG_ADMIN_EMAIL=admin@ggplay.gg
GG_ADMIN_PASSWORD=ggplay-admin
